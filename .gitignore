# It is recommended that django migrations NOT be excluded from commits, but
# that makes no sense when we're excluding the database: 'git checkout <branch>'
# can replace one but not the other.
db.sqlite3
main/migrations/
abcdb/__pycache__/
main/__pycache__/
Cargo.lock
target/

# symlink to file in root directory
docs/README.rst

# various mess
.coveragerc
.idea/
_abc/
abcdb/debug-SqlPrintingMiddleware.py
abcdb/settings-postgresql.py
dumps/
stuff/
